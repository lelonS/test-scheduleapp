import { Given, When, Then } from "@badeball/cypress-cucumber-preprocessor";

Given('I am on the page {string}', (a) => {
  // TODO: implement step
});

When('I click on the link with aria-label {string}', (a) => {
  // TODO: implement step
});

/* No duplicate steps, this one already in admin-nav.js
Then('I get sent to the page {string}', (a) => {});*/

/* No duplicate steps, this one already above
Given('I am on the page {string}', (a) => {});*/

When('I fill in input id {string} with {string}', (a, b) => {
  // TODO: implement step
});

/* No duplicate steps, this one already above
When('I fill in input id {string} with {string}', (a, b) => {});*/

/* No duplicate steps, this one already above
When('I fill in input id {string} with {string}', (a, b) => {});*/

/* No duplicate steps, this one already above
When('I fill in input id {string} with {string}', (a, b) => {});*/

/* No duplicate steps, this one already above
When('I fill in input id {string} with {string}', (a, b) => {});*/

/* No duplicate steps, this one already above
When('I fill in input id {string} with {string}', (a, b) => {});*/

When('I click on the div with id {string}', (a) => {
  // TODO: implement step
});

When('I click on the li with data-value {string}', (a) => {
  // TODO: implement step
});

/* No duplicate steps, this one already above
When('I click on the div with id {string}', (a) => {});*/

/* No duplicate steps, this one already above
When('I click on the li with data-value {string}', (a) => {});*/

When('I click on the input id {string}', (a) => {
  // TODO: implement step
});

When('I click on the button with aria-label {string}', (a) => {
  // TODO: implement step
});

/* No duplicate steps, this one already in admin-nav.js
Then('I get sent to the page {string}', (a) => {});*/

When('I click on the span with data-field {string}', (a) => {
  // TODO: implement step
});

Then('the first row of the tbody with class {string} contains {string}, {string} and {string}', (a, b, c, d) => {
  // TODO: implement step
});

/* No duplicate steps, this one already above
Given('I am on the page {string}', (a) => {});*/

/* No duplicate steps, this one already above
When('I click on the span with data-field {string}', (a) => {});*/

When('I click on the first edit button', () => {
  // TODO: implement step
});

/* No duplicate steps, this one already above
When('I fill in input id {string} with {string}', (a, b) => {});*/

/* No duplicate steps, this one already above
When('I fill in input id {string} with {string}', (a, b) => {});*/

/* No duplicate steps, this one already above
When('I fill in input id {string} with {string}', (a, b) => {});*/

/* No duplicate steps, this one already above
When('I click on the button with aria-label {string}', (a) => {});*/

/* No duplicate steps, this one already in admin-nav.js
Then('I get sent to the page {string}', (a) => {});*/

/* No duplicate steps, this one already above
When('I click on the button with aria-label {string}', (a) => {});*/

/* No duplicate steps, this one already above
Then('the first row of the tbody with class {string} contains {string}, {string} and {string}', (a, b, c, d) => {});*/

/* No duplicate steps, this one already above
Given('I am on the page {string}', (a) => {});*/

/* No duplicate steps, this one already above
When('I click on the span with data-field {string}', (a) => {});*/

/* No duplicate steps, this one already above
When('I click on the first edit button', () => {});*/

/* No duplicate steps, this one already above
When('I click on the button with aria-label {string}', (a) => {});*/

/* No duplicate steps, this one already in admin-nav.js
Then('I get sent to the page {string}', (a) => {});*/

/* No duplicate steps, this one already above
When('I click on the button with aria-label {string}', (a) => {});*/

Then('the first row of the tbody with class {string} does not contain {string}, {string} and {string}', (a, b, c, d) => {
  // TODO: implement step
});